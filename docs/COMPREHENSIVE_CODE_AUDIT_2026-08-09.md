# 智园领航全面代码审查与优化清单

审查日期：2026-08-09  
审查分支：`codex/comprehensive-optimization-20260809`

## 口径

- 严重程度：严重 / 高 / 中 / 低。
- 工作量：S（半天内）、M（1—2 天）、L（3—7 天）、XL（跨迭代）。
- “已完成”表示本分支已实现并纳入回归；“后续架构项”表示不适合在一次安全修复中强行改造，但风险已明确记录。

## 按优先级排序的清单

| 优先级 | 维度 | 问题 | 严重程度 | 预估工作量 | 状态与处理 |
|---|---|---|---:|---:|---|
| P0 | 安全 | 数据库异常时认证和权限检查会回退到 JWT 或直接放行 | 严重 | S | 已完成：数据库启用后以数据库账户/角色为准；异常返回 503，禁用或删除账户返回 401；权限服务默认拒绝。 |
| P0 | 安全 | 任务、Trace、Checkpoint 和自定义 conversation_id 缺少归属校验，存在越权读取/覆盖风险 | 严重 | M | 已完成：统一任务归属校验；普通用户只能访问自己的状态；会话读写校验 owner；跨用户对象统一返回 404。 |
| P0 | 依赖 | Python 审计发现 69 个已知漏洞，涉及 python-jose/ecdsa、Starlette、multipart、pypdf、Pillow 等；npm 有 3 个漏洞 | 严重 | M | 已完成：改用 PyJWT，升级 FastAPI/Starlette、multipart、PDF/图像和前端依赖；两侧漏洞扫描均为 0。 |
| P0 | 安全/性能 | 文档上传一次性读入内存，临时文件名可碰撞；Office 压缩炸弹、伪装格式和超大正文无限制 | 高 | M | 已完成：1 MB 流式写入、25 MB 上限、随机临时文件、阻塞解析移出事件循环；加入格式签名、解压大小/成员数、PDF 页数和正文上限。 |
| P0 | 安全 | 政策爬虫只校验初始 URL；重定向可离开白名单，响应体无上限 | 高 | S | 已完成：仅允许 HTTPS、443、无用户信息的 `gz.gov.cn` 域名；重定向后再次校验；响应流限制 10 MB；补 SSRF 变体测试。 |
| P1 | 安全 | `DATABASE_ENABLED=false` 时 RBAC 权限检查直接允许所有角色 | 高 | S | 已完成：轻量部署也使用同一角色权限矩阵，viewer 不能调用管理权限。 |
| P1 | 安全 | 生产配置允许误关认证/数据库，允许通配 CORS 或不安全 JWT 算法 | 高 | S | 已完成：生产启动校验强制数据库、认证、强密钥、明确 CORS 和 HS256/384/512。 |
| P1 | 安全 | 刷新令牌通过查询参数传输，可能进入 URL/代理日志；登录跳转接受 `//host` | 高 | S | 已完成：刷新令牌改为 JSON 请求体并轮换返回；修复协议相对开放跳转；前端自动刷新。 |
| P1 | 安全 | 登录无抗暴力破解；不存在用户路径未执行 bcrypt，存在账户枚举时序差异 | 高 | S | 已完成：Nginx 登录接口按 IP 限流；不存在/禁用用户也执行固定虚拟 bcrypt 校验。 |
| P1 | 安全 | SSE 任务令牌会进入 Nginx 访问日志，后台异常原文会推送到客户端 | 高 | S | 已完成：流接口关闭 access log；客户端只收到通用错误，详细堆栈保留服务端日志。 |
| P1 | 性能 | 仪表盘业务指标一次请求执行多次聚合，多页面相邻请求重复执行相同查询 | 中 | M | 已完成：加入 15 秒并发安全、返回值隔离的聚合缓存；补缓存合并测试。 |
| P1 | 性能 | 风险、政策日期、产业分布、Agent 执行和会话历史高频查询缺少索引 | 高 | M | 已完成：新增 Alembic `009_business_indexes`，覆盖 8 组热点索引。 |
| P1 | 性能 | 管理员用户列表逐用户查询角色，形成 N+1 | 中 | S | 已完成：单次外连接加载账户和角色，并兼容异常的多角色记录。 |
| P1 | 数据完整性 | 园区资料 JSON 索引为无锁读改写，重复导入或并发删除可能丢记录；临时索引名固定 | 高 | M | 已完成：单进程部署下使用可重入锁覆盖原子操作，索引使用唯一临时文件原子替换。 |
| P1 | 前端 | Chat/Workspace 离开页面时 EventSource 不关闭；旧 DAG live 分支没有流令牌且实际不可用 | 中 | S | 已完成：持有并清理 EventSource；删除未使用且协议错误的 live 分支；Trace 请求支持 AbortController。 |
| P1 | 前端安全 | Access/refresh token 长期保存在 localStorage，扩大持久 XSS 后的凭据暴露窗口 | 高 | M | 部分完成：统一存储模块并迁移至 sessionStorage，退出时同时清理旧存储；完整 HttpOnly Cookie/BFF 方案列为后续架构项。 |
| P1 | 可维护性 | 缺少持续集成、依赖漏洞门禁和自动更新机制 | 高 | M | 已完成：GitHub Actions 执行 Ruff、pip-audit、pytest、npm audit、typecheck 和生产构建；Dependabot 每周检查三类依赖。 |
| P2 | 代码质量 | 35 处未使用导入/死变量；多个未引用 API、store、Tailwind 配置和直接依赖 | 中 | S | 已完成：Ruff `F` 规则清零；删除 4 个死代码模块、旧 store、Tailwind 配置及 5 个未使用运行时依赖。 |
| P2 | 前端性能 | Context Provider 每次渲染创建新函数和对象，扩大无意义重渲染 | 中 | S | 已完成：稳定 DataMode 常量；UserContext 使用 `useCallback`/`useMemo`。 |
| P2 | 安全 | 缺少基础响应安全头，公开详细健康接口可能泄露模型/内部错误 | 中 | S | 已完成：Next/Nginx 增加基础安全头；详细健康与 warmup 仅管理员可读；不再返回原始 warmup 错误。 |
| P2 | 可维护性 | README 含过时版本、绝对路径、旧演示数据数量和失效约束 | 中 | S | 已完成：重写启动、部署、验证、导入格式与数据合规说明。 |
| P2 | 数据正确性 | ORM JSONB 字段使用共享 `{}` 默认值 | 中 | S | 已完成：改为 `default=dict`。 |

## 后续架构项

| 优先级 | 问题 | 严重程度 | 工作量 | 建议 |
|---|---|---:|---:|---|
| P1 | 园区资料、企业和部分业务表尚未形成完整 `park_id` 行级隔离 | 高（多园区时） | XL | 当前适合单园区部署。扩展多园区前，应把 park_id 写入主数据、所有查询条件和数据库约束，并加入跨园区越权测试。 |
| P2 | 浏览器令牌仍可被同源 XSS 读取 | 中 | L | 采用同源 BFF 或后端 HttpOnly + Secure + SameSite Cookie，增加 CSRF 防护和 refresh-token 服务端撤销表。 |
| P2 | `investment_decision_graph.py`、`dashboard_service.py` 和招商页面体积过大 | 中 | L | 按“取数/规则/编排/序列化”和页面业务区块拆分；先补快照/组件测试，再逐步迁移，避免一次性重构引入业务回归。 |
| P2 | 前端缺少独立组件测试和浏览器 E2E | 中 | L | 增加 Vitest + Testing Library；使用 Playwright 覆盖登录、资料导入、工作台、政策审核、权限申请与招商闭环。 |
| P2 | JSON 文件资料库的锁只覆盖当前单进程部署 | 中（多 worker 时） | M | 若增加 Uvicorn worker 或多副本，应把资料索引迁移至 PostgreSQL，或使用跨进程文件锁/分布式锁。 |
| P3 | 大量旧类型注解、导入排序和宽泛异常仍未纳入强制 Ruff 规则 | 低—中 | M | 当前先启用会影响正确性的 `F` 规则；后续按模块逐步启用 I、UP、B、RUF，避免产生难以审阅的大型机械差异。 |
| P3 | Redis 5、pgvector Python 0.3、Ant Design 5 等存在主版本升级 | 低（当前无已知漏洞） | L | 保持当前兼容版本，由 Dependabot 提示；在独立分支升级并执行 UI/E2E 与真实数据库回归，不应混入安全热修。 |

## 验证记录

- 优化前：后端 `142 passed, 1 skipped`；前端生产构建通过。
- 优化前依赖审计：Python 69 个已知漏洞；npm 3 个漏洞。
- 优化后：后端 `159 passed`；Ruff 全部通过；`pip-audit` 未发现已知漏洞。
- 前端：`npm run typecheck` 与 Next.js 生产构建通过；`npm audit` 未发现已知漏洞。
- 部署：Docker Compose 配置与 Nginx 配置通过；Alembic 已升级到 `009_business_indexes`；运行容器健康。
- 运行验证：浏览器覆盖主要页面及政策企业匹配弹窗，页面无控制台错误；政策反查范围已从招商候选池扩展到含导入资料的全园区企业目录，并区分初步与正式资格结论；公开就绪接口返回 200，详细健康接口匿名访问返回 401，安全响应头生效。
